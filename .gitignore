# Build output
/build/
**/build/
/.gradle/
**/.gradle/
/.gradle-home/
/.gradle-agent-cache/
/.kotlin/
/out/
/.scratch/

# IDE
/.idea/
*.iml
*.iws
*.ipr
.claude/
.zcode/

# OS generated
.DS_Store
Thumbs.db
*.swp
*~

# Runtime artifacts (config with credentials, local DB, image cache)
/config/
**/config/*.toml
chat.db
dat/
*.prompt

# HAR captures may contain session cookies / login bodies — never commit
*.har

# Misc downloads
*.html
*.url

/local.properties

# Android signing keys — never commit
*.jks
*.keystore
keystore.properties

# Agent tooling artifacts — never commit
.dsh-vision-router/

# Local sandbox
/run/
/debug-sandbox
/temp/
